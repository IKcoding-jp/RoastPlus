# タスクリスト

**Issue**: #219
**作成日**: 2026-02-11

## フェーズ1: Modal/Dialogフォーカストラップ実装

- [ ] `components/ui/Modal.tsx` のコードを確認
- [ ] フォーカストラップライブラリの選定（`focus-trap-react` or `react-focus-lock`）
- [ ] `Modal.tsx` にフォーカストラップを実装
- [ ] モーダル開閉時のフォーカス移動をテスト
- [ ] `components/ui/Dialog.tsx` も同様にフォーカストラップを実装
- [ ] キーボードテスト（Tab/Shift+Tab）で動作確認

## フェーズ2: エラーメッセージのライブリージョン対応

- [ ] `app/login/page.tsx:164-166` のエラーメッセージ箇所を確認
- [ ] エラーメッセージに `role="alert"` を追加
- [ ] スクリーンリーダーで読み上げ動作を確認
- [ ] 他のページでも同様のエラーメッセージパターンがあれば対応

## フェーズ3: スキップリンク実装

- [ ] `app/layout.tsx` の冒頭にスキップリンクコンポーネントを設計
- [ ] `<a href="#main-content">メインコンテンツへスキップ</a>` を追加
- [ ] メインコンテンツ領域に `id="main-content"` を付与
- [ ] スキップリンクのスタイリング（フォーカス時のみ表示）
- [ ] キーボード操作でスキップリンクが機能することを確認

## フェーズ4: ナビゲーション要素のセマンティック化

- [ ] ナビゲーション要素（ヘッダーメニュー等）を特定
- [ ] `<nav>` タグで囲む
- [ ] `aria-label` でナビゲーション領域を説明
- [ ] 他のページでも同様のナビゲーション要素があれば対応

## フェーズ5: axe-coreテストの厳格化

- [ ] `e2e/accessibility/a11y.spec.ts:35` を確認
- [ ] `serious` 違反のログ出力部分をアサーションに変更
- [ ] `expect(violations.filter(v => v.impact === 'serious')).toHaveLength(0)` を追加
- [ ] E2Eテストを実行し、違反がないことを確認
- [ ] 違反があれば修正

## フェーズ6: 検証

- [ ] `npm run build` でビルドが成功することを確認
- [ ] `npm run lint` でLintエラーがないことを確認
- [ ] `npm run test` で全テストが通ることを確認
- [ ] E2Eアクセシビリティテストが全て合格することを確認
- [ ] スクリーンリーダー（NVDA/VoiceOver）で主要機能をテスト

## 依存関係

- フェーズ1〜4は並行実行可能（各実装が独立）
- フェーズ5はフェーズ1〜4完了後に実行（axe-coreテストで検出）
- フェーズ6は全フェーズ完了後に検証

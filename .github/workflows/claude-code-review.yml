name: Claude Code Review

on:
  workflow_dispatch:

jobs:
  claude-review-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Claude review is disabled
        run: echo "Claude Code Review is disabled by default for faster solo development."
